import { ArrowUpRight } from 'lucide-react'
interface Props { eyebrow: string; title: string; href?: string; action?: string; invert?: boolean }
export function SectionHeading({ eyebrow, title, href = '#', action = 'Xem tất cả', invert = false }: Props) { return <div className="mb-7 flex items-end justify-between gap-4 sm:mb-9"><div className="border-l-4 border-[#e1252d] pl-3"><p className={`mb-1 text-[11px] font-bold tracking-[.16em] ${invert ? 'text-[#ffe36b]' : 'text-[#0758a7]'}`}>{eyebrow}</p><h2 className={`font-display text-2xl font-bold tracking-[-.03em] sm:text-3xl ${invert ? 'text-white' : 'text-slate-950'}`}>{title}</h2></div><a className={`group hidden items-center gap-1 text-sm font-semibold sm:inline-flex ${invert ? 'text-white' : 'text-[#0758a7]'}`} href={href}>{action}<ArrowUpRight size={17} className="transition-transform group-hover:-translate-y-0.5 group-hover:translate-x-0.5" /></a></div> }
