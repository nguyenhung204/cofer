import { ArrowUpRight } from 'lucide-react'
import type { News } from '../../types'
export function NewsCard({ item, featured = false }: { item: News; featured?: boolean }) { return <article className={`group overflow-hidden rounded-2xl border border-slate-200 bg-white transition-all duration-200 hover:-translate-y-1 hover:shadow-[0_14px_30px_rgba(15,40,82,.10)] ${featured ? 'md:row-span-2' : ''}`}><a href="#" className="block" aria-label={item.title}><div className={`overflow-hidden ${featured ? 'aspect-[16/9] md:aspect-[1.35/1]' : 'aspect-video'}`}><img src={item.image} alt="" width="900" height="506" loading="lazy" className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-[1.04]" /></div><div className={`p-4 ${featured ? 'sm:p-6' : ''}`}><div className="mb-3 flex items-center justify-between gap-2 text-[10px] font-bold tracking-[.13em] text-blue-700"><span>{item.category}</span><time className="shrink-0 text-slate-400">{item.date}</time></div><h3 className={`font-display font-bold leading-snug tracking-[-.025em] text-slate-950 ${featured ? 'text-xl sm:text-2xl' : 'text-[17px]'}`}>{item.title}</h3>{featured && <p className="mt-3 hidden text-sm leading-relaxed text-slate-600 sm:block">{item.excerpt}</p>}{featured && <span className="mt-5 inline-flex items-center gap-1 text-sm font-bold text-blue-700">Đọc thêm <ArrowUpRight size={16} /></span>}</div></a></article> }
