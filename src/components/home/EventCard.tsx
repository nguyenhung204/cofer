import { ArrowRight, Clock3, MapPin } from 'lucide-react'
import type { Event } from '../../types'
export function EventCard({ event }: { event: Event }) { return <article className="group flex gap-4 rounded-2xl border border-white/10 bg-white/[.06] p-4 text-white hover:bg-white/[.1] sm:items-center sm:gap-5 sm:p-5"><time className="flex w-[58px] shrink-0 flex-col items-center rounded-xl bg-white px-2 py-2.5 text-blue-950"><strong className="font-display text-2xl font-extrabold leading-none">{event.day}</strong><span className="mt-1 text-[10px] font-black tracking-wider text-blue-600">{event.month}</span></time><div className="min-w-0 flex-1"><p className="mb-1 text-[10px] font-bold tracking-[.13em] text-blue-200">{event.kind}</p><h3 className="font-display text-[17px] font-bold leading-snug">{event.title}</h3><div className="mt-3 flex flex-wrap gap-x-4 gap-y-1 text-xs text-blue-100"><span className="inline-flex items-center gap-1"><MapPin size={14} />{event.place}</span><span className="inline-flex items-center gap-1"><Clock3 size={14} />{event.time}</span></div></div><a href="#" className="hidden min-h-11 shrink-0 items-center gap-1 rounded-xl border border-white/25 px-3 text-sm font-semibold hover:bg-white hover:text-blue-950 md:inline-flex">Chi tiết <ArrowRight size={15} /></a></article> }
