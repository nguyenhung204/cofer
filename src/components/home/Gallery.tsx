import { X } from 'lucide-react'
import { useState } from 'react'
import { gallery } from '../../data/gallery'
import { SectionHeading } from '../ui/SectionHeading'
import type { GalleryItem } from '../../types'
export function Gallery() { const [selected, setSelected] = useState<GalleryItem | null>(null); return <section className="section-space" id="thu-vien"><div className="page-shell"><SectionHeading eyebrow="KHOẢNH KHẮC COFER" title="THƯ VIỆN HÌNH ẢNH" action="Xem thư viện" /><div className="grid grid-cols-2 gap-3 md:auto-rows-[145px] md:grid-cols-4">{gallery.map(item => <button type="button" key={item.id} onClick={() => setSelected(item)} className={`group relative aspect-[4/3] overflow-hidden rounded-2xl text-left md:aspect-auto ${item.span ?? ''}`}><img src={item.image} alt={item.alt} loading="lazy" width="900" height="675" className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-[1.05]" /><span className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-slate-950/75 to-transparent px-3 pb-3 pt-8 text-xs font-semibold text-white opacity-0 transition-opacity group-hover:opacity-100">{item.alt}</span></button>)}</div></div>{selected && <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/90 p-4" role="dialog" aria-modal="true" aria-label={selected.alt} onClick={() => setSelected(null)}><div className="relative max-h-full max-w-4xl" onClick={event => event.stopPropagation()}><img src={selected.image} alt={selected.alt} className="max-h-[80vh] w-auto max-w-full rounded-2xl object-contain" /><button type="button" onClick={() => setSelected(null)} className="absolute -right-2 -top-2 grid h-11 w-11 place-items-center rounded-full bg-white text-slate-900 shadow-lg" aria-label="Đóng ảnh"><X /></button><p className="mt-3 text-center text-sm text-white">{selected.alt}</p></div></div>}</section> }
