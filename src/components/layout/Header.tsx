import { Menu, Search, X } from 'lucide-react'
import { useState } from 'react'
import doanLogo from '../../assets/doan-logo.png'
import studentLogo from '../../assets/hoi-sinh-vien-logo.png'
const links = ['Trang chủ', 'Giới thiệu', 'Tin tức', 'Hoạt động', 'Văn bản', 'Thư viện', 'Liên hệ']
function Brand() { return <a href="#top" className="flex min-w-0 items-center gap-2.5" aria-label="Trang chủ Đoàn Thanh niên – Hội Sinh viên COFER"><img src={doanLogo} alt="Huy hiệu Đoàn TNCS Hồ Chí Minh" width="48" height="48" className="h-11 w-11 shrink-0 object-contain" /><img src={studentLogo} alt="Huy hiệu Hội Sinh viên Việt Nam" width="48" height="48" className="h-10 w-10 shrink-0 object-contain" /><span className="min-w-0 leading-tight"><strong className="block truncate text-[10px] font-extrabold tracking-[.035em] text-[#0758a7] sm:text-[11px]">ĐOÀN THANH NIÊN – HỘI SINH VIÊN</strong><span className="block truncate text-[11px] font-semibold text-slate-600">Trường Cao đẳng Kinh tế Đối ngoại</span></span></a> }
export function Header() { const [open, setOpen] = useState(false); return <header className="sticky top-0 z-40 border-b-2 border-[#e1252d] bg-white/95 backdrop-blur"><div className="page-shell flex h-[72px] items-center justify-between gap-3"><Brand /><nav className="hidden items-center gap-5 xl:flex" aria-label="Điều hướng chính">{links.map((link, index) => <a key={link} className={`text-sm font-semibold hover:text-[#0758a7] ${index === 0 ? 'text-[#0758a7]' : 'text-slate-600'}`} href={index === 0 ? '#top' : '#'}>{link}</a>)}</nav><div className="flex items-center gap-1"><button className="grid h-11 w-11 place-items-center rounded-full text-slate-700 hover:bg-blue-50" aria-label="Tìm kiếm" type="button"><Search size={20} /></button><button className="grid h-11 w-11 place-items-center rounded-full text-slate-700 hover:bg-blue-50 xl:hidden" aria-label={open ? 'Đóng menu' : 'Mở menu'} aria-expanded={open} type="button" onClick={() => setOpen(!open)}>{open ? <X size={23} /> : <Menu size={23} />}</button></div></div>{open && <nav className="border-t border-slate-100 bg-white px-4 py-3 shadow-lg xl:hidden" aria-label="Điều hướng di động"><div className="page-shell grid gap-1">{links.map((link, index) => <a key={link} href={index === 0 ? '#top' : '#'} onClick={() => setOpen(false)} className="rounded-lg border-l-2 border-transparent px-4 py-3 text-sm font-semibold text-slate-700 hover:border-[#e1252d] hover:bg-blue-50 hover:text-[#0758a7]">{link}</a>)}</div></nav>}</header> }
